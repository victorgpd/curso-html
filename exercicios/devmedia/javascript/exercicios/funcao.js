const retorna_media = (nota1, nota2) => (nota1 + nota2) /2
const retorna_media2 = (nota1, nota2) => {
    return (nota1 + nota2) / 2
}

let media = retorna_media(8, 4)
console.log(`A média do aluno é: ${media}`)

let media2 = retorna_media2(7, 8)
console.log(`A média do aluno é: ${media2}`)